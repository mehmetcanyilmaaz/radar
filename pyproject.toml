[project]
name = "radar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mehmetcanyilmaaz", email = "mehmet.can.yilmaz.27@dartmouth.edu" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
radar = "radar:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.7",
]
